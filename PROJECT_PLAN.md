# 🗺️ JP-Thai Office Buddy - Project Roadmap

เอกสารนี้สร้างขึ้นเพื่อติดตามความคืบหน้าของโปรเจกต์ (Roadmap) และบอกว่า **"ตอนนี้เราอยู่จุดไหน"** 
เมื่อคุณเปลี่ยนไปทำโปรเจกต์นี้ในคอมพิวเตอร์เครื่องอื่น ให้เข้ามาดูหัวข้อ **"📍 สถานะปัจจุบัน (Current Status)"** เป็นอันดับแรกครับ!

---

## 🎯 1. รายละเอียดโปรเจกต์ (Project Overview)
สร้างระบบ AI Assistant (ด้วย Python Flask + Gemini API) สำหรับโรงงานและอุตสาหกรรมไม้ (Timber Industry) โดยมีฟีเจอร์หลักคือ:
*   แปลภาษาไทย-ญี่ปุ่น 3 ระดับ (Casual, Polite, Business)
*   แปลและตรวจสอบเนื้อหาเอกสารญี่ปุ่นอย่างละเอียด
*   เขียนฟอร์มเอกสาร (เช่น อีเมลหาลอตเตอรี่, ใบลา) ด้วยภาษาที่เหมาะสม
*   พจนานุกรมคำศัพท์โรงเลื่อย/ศัพท์ช่างไม้
*   สแกนอ่านตัวหนังสือญี่ปุ่นจากรูปภาพ (Vision OCR)

---

## 📍 2. สถานะปัจจุบัน (Current Status & Next Steps)

ขณะนี้โปรเจกต์ได้วางระบบ Backend (`app.py`) เสร็จสิ้นแล้ว **"แต่ยังไม่ได้ทำการทดสอบความถูกต้องของ AI"**

### 👉 เฟส 1: ทดสอบและปรับจูนความฉลาดของ AI (★ เรากำลังอยู่ที่นี่!)
สิ่งที่คุณต้องทำเป็นอันดับแรกในขั้นตอนนี้คือ:
- [ ] รันโปรแกรม (ผ่าน `launch_emulator.ps1` หรือรันไฟล์ `app.py` โดยตรง)
- [ ] ลองใช้งาน **ระบบแปลภาษา** ว่าสุภาพและตรงบริบทโรงงานไม้หรือไม่
- [ ] ลองใช้งาน **ระบบตรวจเอกสาร** ว่าแปลกลับได้ครบถ้วน ไม่ตกหล่นหรือไม่
- [ ] ลองใช้งาน **ระบบสร้างเอกสารอีเมล** ดูฟอร์มการอักษรว่าสวยและทางการพอไหม
- [ ] ลองใช้งาน **การค้นหาศัพท์อุตสาหกรรม (Glossary)** เช่น คำว่า BZI, KAK, พรีคัต
- [ ] หากพบว่า AI ตอบแปลกๆ ให้คุณเข้าไปแก้ไข Prompt (คำสั่งสอน AI) ในไฟล์ `app.py` 

**⚠️ กฎเหล็ก:** ห้ามข้ามไปทำแอปหรือเอาขึ้นเซิร์ฟเวอร์ จนกว่าจะมั่นใจว่าคำตอบที่หน้าเว็บทำงานได้ถูกต้อง 100%

---

## 🚀 3. แผนงานในอนาคต (Future Roadmap)

เมื่อคุณทดสอบเฟส 1 จนพอใจแล้ว ให้ลุยตามแผนด้านล่างนี้ตามลำดับ:

### เฟส 2: นำระบบขึ้น Cloud (Deployment)
เพื่อให้ระบบรัน 24 ชม. และไม่ต้องสั่งรัน `app.py` ด้วยตัวเองอีกต่อไป
- [ ] สมัคร Cloud Server ฟรี (ผมแนะนำ Render.com หรือ PythonAnywhere)
- [ ] นำโค้ดชุดนี้อัปโหลดและเปิดรันบน Cloud
- [ ] จะได้ URL จริง (เช่น `https://jp-thai-buddy.onrender.com`) มาใช้งาน

### เฟส 3: ตกแต่งหน้าเว็บให้เหมือนแอป (PWA - Progressive Web App)
- [ ] ขัดเกลาหน้าตาของ `templates/index.html` และ CSS ให้สวยงาม ปุ่มใหญ่ กดง่าย
- [ ] เพิ่มไฟล์ Manifest เพื่อให้ผู้ใช้สามารถกด **"Add to Home Screen (เพิ่มไปยังหน้าจอหลัก)"** บนมือถือได้ ซึ่งมันจะเปลี่ยนหน้าเว็บเราให้กลายเป็นไอคอนแอปบนมือถือทันที (ใช้งานง่ายและทำเสร็จไวที่สุด)

### เฟส 4: พัฒนาแอปผ่าน Flutter (ทางเลือกเสริม)
- [ ] หากทำจบเฟส 3 แล้วรู้สึกว่ายังอยากได้เป็นแอปมือถือลงเครื่องแบบสมบูรณ์ ให้ไปพัฒนาต่อในโฟลเดอร์ `mobile_app/`
- [ ] เขียนโค้ด Flutter เพื่อดึง API จากลิงก์ Cloud ในเฟส 2 มาใช้
- [ ] ทดสอบและ Export ออกเป็นไฟล์ APK สำหรับ Android
